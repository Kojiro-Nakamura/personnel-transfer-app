# 実装計画: 職員一括編集のHTMLエクスポート機能

職員一括編集画面の内容を、閲覧・ソート機能付きの単独HTMLファイルとして保存する機能を実装します。

## 概要

一括編集画面の表組み・デザインを再現した、スタンドアロンで動作するHTMLファイルを生成してダウンロードさせます。編集は不要ですが、一覧の確認や共有に便利な形式にします。

## 実装内容

### 1. `BulkEditModal` へのHTML保存ボタン追加
* 「CSV保存」ボタンの横に「HTML保存」ボタンを追加します。

### 2. HTML生成ロジック (`handleExportHTML`) の作成
以下の要件を満たすHTML文字列を動的に生成し、ダウンロードさせます：

* **データ構造**:
  * 現在画面に表示されている `sortedEmps` の内容を元にテーブルを生成します。
  * 編集用の入力フォーム（`input`）は除去し、純粋なテキスト（`div`や`span`）として出力します。
  * チェックボックス列は閲覧用途では不要なため省略します。
* **デザイン・スタイル**:
  * 画面上と近い見た目になるよう、基本的なCSS（Tailwindの出力結果に近いもの）を `<style>` タグに埋め込みます。
  * **氏名列の固定**: ユーザーの要望通り、氏名列に `position: sticky; left: 0;` を適用し、横スクロールしても常に見えるようにします。
* **ソート機能**:
  * HTML内に軽量なJavaScriptを埋め込みます。
  * テーブルのヘッダー（`<th>`）をクリックした際に、その列のテキスト内容（または属性値）に基づいて行（`<tr>`）を並び替える機能を実装します。

## 懸念点・確認事項

> [!NOTE]
> HTML出力時は画面上と全く同じTailwind CSSを読み込ませることは容量の都合上難しいため、表の枠線や背景色、文字サイズなど主要なスタイルのみを抽出した軽量なCSSを直接埋め込む形となります。（おおむね近い見た目にはなります）

> [!IMPORTANT]
> チェックボックス列は出力から省き、一番左の固定列を「氏名」としますがよろしいでしょうか？

上記の内容で実装を進めてよろしいでしょうか？「Proceed」またはフィードバックをお願いいたします。
